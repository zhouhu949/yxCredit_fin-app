apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'maven'

version '1.0'

dependencies {
    compile project(":dao-base")
    compile project(":fin-util")
    compile project(":fin-model")
}

jar {
    baseName = 'fin-mapper'
}
